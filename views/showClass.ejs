<h1>Class name</h1>

<h2> Students in the class</h2> <button type="button" name="button">delete</button>



<ul >
  <% enrollments.forEach(function(val){ %>
    <% val = val._student%>
  <li id='<%=val._id %>'><%= val.FirstName%> <%= val.LastName%>
    <div>
    <button type="button" name="button" class="btn btn-primary">Edit</button>
    <button class="btn btn-primary delete" type="button" name="button" data-id='<%= val._id %>' data-type='/api/students/' >Delete</button>
    </div
  </li>
  <% }); %>
</ul>



<% if(true){ %>
   <h1> No students to show</h1>
 <% } else{ %>
   <ul >
     <% enrollments.forEach(function(val){ %>
       <% val = val._student%>
     <li id='<%=val._id %>'><%= val.FirstName%> <%= val.LastName%>
       <div>
       <button type="button" name="button" class="btn btn-primary">Edit</button>
       <button class="btn btn-primary delete" type="button" name="button" data-id='<%= val._id %>' data-type='/api/students/' >Delete</button>
       </div
     </li>
     <% }); %>
   </ul>
<% } %>

<a href="/students"> Dashboard</a>
